<%= form_for(@note, remote: true, :authenticity_token => true, html: { class: 'new_note', style: "padding-top: 12px; border-top: black solid 1px; position: absolute; top: #{params[:ypos]}px; left: #{params[:xpos]}px;"}) do |f| %>
  <% if @note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@note.errors.count, "error") %> prohibited this note from being saved:</h2>

      <ul>
      <% @note.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field :xpos %>
  <%= f.hidden_field :ypos %>
  <%= f.hidden_field :width %>
  <%= f.hidden_field :height %>
  <div class="field">
    <%= f.text_area :text %>
  </div>
<% end %>
